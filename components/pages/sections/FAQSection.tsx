import React from 'react';
import Container from "@/components/layout/Container";

const FaqSection = () => {
    return (
        <section id={'FAQ'} className={'bg-black text-white min-h-screen'}>
            <Container className={'py-20'}>
                <h1>FAQ</h1>
            </Container>
        </section>
    );
};

export default FaqSection;