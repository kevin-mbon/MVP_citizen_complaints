import React from 'react';

const ItemSeparator = () => {
    return (
        <div className={'bg-gradient-to-r from-transparent via-slate-300 to-transparent h-[1px] w-full'}>
        </div>
    );
};

export default ItemSeparator;